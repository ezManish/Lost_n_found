<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %></title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css">
    <style>
        .btn-loading {
            position: relative;
            pointer-events: none;
        }

        .btn-loading .fa-spinner {
            margin-right: 8px;
        }

        .card {
            transition: all 0.3s ease;
        }

        .card:hover {
            transform: translateY(-5px);
            box-shadow: 0 8px 25px rgba(0,0,0,0.15);
        }        
        :root {
            --primary-color: #2c3e50;
            --secondary-color: #3498db;
            --accent-color: #e74c3c;
            --light-color: #ecf0f1;
            --gradient-primary: linear-gradient(135deg, #2c3e50 0%, #3498db 100%);
            --gradient-secondary: linear-gradient(135deg, #3498db 0%, #2c3e50 100%);
            --gradient-accent: linear-gradient(135deg, #e74c3c 0%, #c0392b 100%);
            --gradient-success: linear-gradient(135deg, #27ae60 0%, #2ecc71 100%);
            --gradient-card: linear-gradient(135deg, #ffffff 0%, #f8f9fa 100%);
            --gradient-stats: linear-gradient(135deg, #ecf0f1 0%, #ffffff 100%);
        }

        body {
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            background: linear-gradient(135deg, #f8f9fa 0%, #e9ecef 50%, #f8f9fa 100%);
            background-attachment: fixed;
            min-height: 100vh;
        }

        .navbar {
            background: var(--gradient-primary);
            box-shadow: 0 2px 15px rgba(44, 62, 80, 0.1);
        }

        .hero-section {
            background: linear-gradient(135deg, rgba(44, 62, 80, 0.85) 0%, rgba(52, 152, 219, 0.85) 100%), 
                        url('https://source.unsplash.com/random/1600x900/?university,campus') no-repeat center center;
            background-size: cover;
            color: white;
            padding: 120px 0;
            text-align: center;
            position: relative;
            overflow: hidden;
        }

        .hero-section::before {
            content: '';
            position: absolute;
            top: 0;
            left: 0;
            right: 0;
            bottom: 0;
            background: linear-gradient(45deg, transparent 0%, rgba(255,255,255,0.1) 100%);
            pointer-events: none;
        }

        .card {
            border: none;
            border-radius: 15px;
            background: var(--gradient-card);
            box-shadow: 0 6px 20px rgba(0, 0, 0, 0.08);
            transition: all 0.4s cubic-bezier(0.175, 0.885, 0.32, 1.275);
            margin-bottom: 25px;
            overflow: hidden;
            border: 1px solid rgba(52, 152, 219, 0.1);
        }

        .card:hover {
            transform: translateY(-8px) scale(1.02);
            box-shadow: 0 12px 35px rgba(44, 62, 80, 0.15);
        }

        .item-image {
            height: 200px;
            object-fit: cover;
            border-top-left-radius: 15px;
            border-top-right-radius: 15px;
            transition: transform 0.3s ease;
        }

        .card:hover .item-image {
            transform: scale(1.05);
        }

        .badge-lost {
            background: var(--gradient-accent);
            border: none;
            font-weight: 600;
            letter-spacing: 0.5px;
        }

        .badge-found {
            background: var(--gradient-secondary);
            border: none;
            font-weight: 600;
            letter-spacing: 0.5px;
        }

        .badge-success {
            background: var(--gradient-success);
            border: none;
            font-weight: 600;
        }

        .filter-section {
            background: var(--gradient-card);
            border-radius: 15px;
            padding: 25px;
            box-shadow: 0 6px 20px rgba(0, 0, 0, 0.08);
            margin-bottom: 30px;
            border: 1px solid rgba(52, 152, 219, 0.1);
        }

        .match-highlight {
            position: relative;
            border: 2px solid transparent;
            background: linear-gradient(white, white) padding-box,
                        linear-gradient(135deg, #2ecc71, #27ae60) border-box;
            border-radius: 15px;
        }

        .match-highlight::before {
            content: "✨ Potential Match!";
            position: absolute;
            top: -12px;
            right: 15px;
            background: var(--gradient-success);
            color: white;
            padding: 4px 12px;
            border-radius: 20px;
            font-size: 0.75rem;
            font-weight: 600;
            z-index: 10;
            box-shadow: 0 4px 15px rgba(46, 204, 113, 0.3);
        }

        .stats-card {
            text-align: center;
            padding: 25px 20px;
            background: var(--gradient-stats);
            border-radius: 15px;
            box-shadow: 0 6px 20px rgba(0, 0, 0, 0.08);
            border: 1px solid rgba(52, 152, 219, 0.1);
            transition: all 0.3s ease;
            position: relative;
            overflow: hidden;
        }

        .stats-card::before {
            content: '';
            position: absolute;
            top: 0;
            left: 0;
            right: 0;
            height: 4px;
            background: var(--gradient-primary);
        }

        .stats-card:hover {
            transform: translateY(-5px);
            box-shadow: 0 10px 30px rgba(44, 62, 80, 0.15);
        }

        .stats-number {
            font-size: 2.8rem;
            font-weight: bold;
            background: var(--gradient-primary);
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
            background-clip: text;
            margin-bottom: 10px;
        }

        footer {
            background: var(--gradient-primary);
            color: white;
            padding: 40px 0 20px;
            margin-top: 80px;
            position: relative;
        }

        footer::before {
            content: '';
            position: absolute;
            top: 0;
            left: 0;
            right: 0;
            height: 4px;
            background: var(--gradient-secondary);
        }

        .alert-auto-dismiss {
            animation: fadeOut 5s forwards;
            border: none;
            border-radius: 10px;
            box-shadow: 0 4px 15px rgba(0, 0, 0, 0.1);
        }

        @keyframes fadeOut {
            0% { opacity: 1; transform: translateY(0); }
            70% { opacity: 1; transform: translateY(0); }
            100% { opacity: 0; display: none; transform: translateY(-20px); }
        }

        /* Enhanced buttons */
        .btn-primary {
            background: var(--gradient-primary);
            border: none;
            border-radius: 8px;
            padding: 12px 30px;
            font-weight: 600;
            transition: all 0.3s ease;
            box-shadow: 0 4px 15px rgba(52, 152, 219, 0.3);
        }

        .btn-primary:hover {
            transform: translateY(-2px);
            box-shadow: 0 6px 20px rgba(52, 152, 219, 0.4);
            background: var(--gradient-secondary);
        }

        .btn-outline-primary {
            border: 2px solid #3498db;
            color: #3498db;
            border-radius: 8px;
            padding: 10px 25px;
            font-weight: 600;
            transition: all 0.3s ease;
            background: transparent;
        }

        .btn-outline-primary:hover {
            background: var(--gradient-primary);
            border-color: transparent;
            transform: translateY(-2px);
            box-shadow: 0 4px 15px rgba(52, 152, 219, 0.3);
        }

        .btn-light {
            background: linear-gradient(135deg, #ffffff 0%, #f8f9fa 100%);
            border: none;
            border-radius: 8px;
            padding: 12px 30px;
            font-weight: 600;
            color: #2c3e50;
            transition: all 0.3s ease;
            box-shadow: 0 4px 15px rgba(255, 255, 255, 0.3);
        }

        .btn-light:hover {
            transform: translateY(-2px);
            box-shadow: 0 6px 20px rgba(255, 255, 255, 0.4);
            background: linear-gradient(135deg, #f8f9fa 0%, #e9ecef 100%);
        }

        /* Form enhancements */
        .form-control, .form-select {
            border-radius: 8px;
            border: 2px solid #e9ecef;
            padding: 12px 15px;
            transition: all 0.3s ease;
        }

        .form-control:focus, .form-select:focus {
            border-color: #3498db;
            box-shadow: 0 0 0 0.2rem rgba(52, 152, 219, 0.25);
            transform: translateY(-1px);
        }

        /* Modal enhancements */
        .modal-content {
            border-radius: 15px;
            border: none;
            box-shadow: 0 20px 60px rgba(0, 0, 0, 0.2);
            background: var(--gradient-card);
        }

        .modal-header {
            background: var(--gradient-primary);
            color: white;
            border-radius: 15px 15px 0 0;
            border: none;
            padding: 20px 25px;
        }

        .modal-header .btn-close {
            filter: invert(1);
        }

        /* Section backgrounds */
        .bg-light {
            background: linear-gradient(135deg, #f8f9fa 0%, #e9ecef 100%) !important;
        }

        /* Load more button enhancement */
        #loadMoreBtn {
            background: var(--gradient-primary);
            border: none;
            padding: 12px 40px;
            border-radius: 25px;
            font-weight: 600;
            transition: all 0.3s ease;
            box-shadow: 0 4px 15px rgba(52, 152, 219, 0.3);
        }

        #loadMoreBtn:hover {
            transform: translateY(-3px) scale(1.05);
            box-shadow: 0 8px 25px rgba(52, 152, 219, 0.4);
            background: var(--gradient-secondary);
        }

        /* Navbar brand enhancement */
        .navbar-brand {
            font-weight: 700;
            font-size: 1.5rem;
            background: linear-gradient(135deg, #ffffff 0%, #ecf0f1 100%);
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
            background-clip: text;
        }

        /* Search button enhancement */
        #searchButton {
            background: var(--gradient-primary);
            border: none;
            border-radius: 0 8px 8px 0;
            padding: 12px 20px;
            transition: all 0.3s ease;
        }

        #searchButton:hover {
            background: var(--gradient-secondary);
            transform: translateY(-1px);
        }

        /* Responsive adjustments */
        @media (max-width: 768px) {
            .hero-section {
                padding: 80px 0;
            }
            
            .stats-card {
                margin-bottom: 20px;
            }
            
            .card:hover {
                transform: translateY(-5px);
            }
        }
    </style>
</head>
<body>
    <!-- Navigation -->
    <nav class="navbar navbar-expand-lg navbar-dark">
        <div class="container">
            <!-- <a class="navbar-brand" href="/">
                <i class="fas fa-search me-2"></i>Lost-n-Found
            </a> -->
            <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav">
                <span class="navbar-toggler-icon"></span>
            </button>
            <div class="collapse navbar-collapse" id="navbarNav">
                <ul class="navbar-nav me-auto">
                    <li class="nav-item">
                        <a class="nav-link active" href="/">Home</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="#" data-bs-toggle="modal" data-bs-target="#postLostModal">Post Lost Item</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="#" data-bs-toggle="modal" data-bs-target="#postFoundModal">Post Found Item</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="#search">Search</a>
                    </li>
                </ul>
                <ul class="navbar-nav">
                    <li class="nav-item">
                        <a class="nav-link" href="/admin/login">
                            <i class="fas fa-user-cog me-1"></i> Admin
                        </a>
                    </li>
                </ul>
            </div>
        </div>
    </nav>

    <!-- Hero Section -->
    <section class="hero-section">
        <div class="container">
            <h1 class="display-4 fw-bold mb-4">Lost Something on Campus?</h1>
            <p class="lead mb-4 fs-5">Our Lost-n-Found system helps reunite students with their lost belongings</p>
            <div class="mt-4">
                <button class="btn btn-light btn-lg me-3 mb-2" data-bs-toggle="modal" data-bs-target="#postLostModal">
                    <i class="fas fa-exclamation-circle me-2"></i> Report Lost Item
                </button>
                <button class="btn btn-outline-light btn-lg mb-2" data-bs-toggle="modal" data-bs-target="#postFoundModal">
                    <i class="fas fa-hand-holding-heart me-2"></i> Report Found Item
                </button>
            </div>
        </div>
    </section>

    <!-- Statistics Section -->
    <section class="py-5">
        <div class="container">
            <div class="row text-center">
                <div class="col-md-3">
                    <div class="stats-card">
                        <div class="stats-number" id="lostCount"><%= stats.lost || 0 %></div>
                        <p class="mb-0 fw-semibold text-muted">Items Lost</p>
                    </div>
                </div>
                <div class="col-md-3">
                    <div class="stats-card">
                        <div class="stats-number" id="foundCount"><%= stats.found || 0 %></div>
                        <p class="mb-0 fw-semibold text-muted">Items Found</p>
                    </div>
                </div>
                <div class="col-md-3">
                    <div class="stats-card">
                        <div class="stats-number" id="matchedCount"><%= stats.resolved || 0 %></div>
                        <p class="mb-0 fw-semibold text-muted">Successful Matches</p>
                    </div>
                </div>
                <div class="col-md-3">
                    <div class="stats-card">
                        <div class="stats-number" id="resolvedCount"><%= stats.total || 0 %></div>
                        <p class="mb-0 fw-semibold text-muted">Total Items</p>
                    </div>
                </div>
            </div>
        </div>
    </section>

    <!-- Search and Filter Section -->
    <section id="search" class="py-4 bg-light">
        <div class="container">
            <div class="filter-section">
                <h3 class="mb-4 text-center">Search Lost-n-Found Items</h3>
                <div class="row">
                    <div class="col-md-8">
                        <div class="input-group mb-3">
                            <input type="text" class="form-control" id="searchInput" placeholder="Search by item name, description...">
                            <button class="btn btn-primary" type="button" id="searchButton">
                                <i class="fas fa-search"></i> Search
                            </button>
                        </div>
                    </div>
                    <div class="col-md-4">
                        <select class="form-select" id="categoryFilter">
                            <option value="">All Categories</option>
                            <option value="Books">Books</option>
                            <option value="Electronics">Electronics</option>
                            <option value="ID Cards">ID Cards</option>
                            <option value="Clothing">Clothing</option>
                            <option value="Accessories">Accessories</option>
                            <option value="Others">Others</option>
                        </select>
                    </div>
                </div>
                <div class="row mt-3">
                    <div class="col-md-4">
                        <select class="form-select" id="typeFilter">
                            <option value="">All Types</option>
                            <option value="lost">Lost Items</option>
                            <option value="found">Found Items</option>
                        </select>
                    </div>
                    <div class="col-md-4">
                        <input type="date" class="form-control" id="dateFilter">
                    </div>
                    <div class="col-md-4">
                        <input type="text" class="form-control" id="locationFilter" placeholder="Filter by location">
                    </div>
                </div>
            </div>
        </div>
    </section>

    <!-- Items Listing Section -->
    <section class="py-5">
        <div class="container">
            <h2 class="mb-4 text-center">Recent Items</h2>
            <div class="row" id="itemsContainer">
                <% if (items && items.length > 0) { %>
                    <% items.forEach(function(item) { %>
                        <div class="col-md-4 mb-4">
                            <div class="card h-100">
                                <img src="<%= item.image || 'https://source.unsplash.com/random/300x200/?' + item.category %>" class="card-img-top item-image" alt="<%= item.title %>" onerror="this.src='https://source.unsplash.com/random/300x200/?<%= item.category %>'">
                                <div class="card-body d-flex flex-column">
                                    <div class="mb-3">
                                        <span class="badge <%= item.type === 'lost' ? 'badge-lost' : 'badge-found' %>">
                                            <%= item.type === 'lost' ? 'Lost' : 'Found' %>
                                        </span>
                                        <% if (item.status === 'resolved') { %>
                                            <span class="badge badge-success ms-1">Resolved</span>
                                        <% } %>
                                    </div>
                                    <h5 class="card-title mb-3"><%= item.title %></h5>
                                    <p class="card-text text-muted flex-grow-1"><%= item.description %></p>
                                    <div class="mt-auto">
                                        <div class="d-flex justify-content-between align-items-center text-muted small mb-2">
                                            <span><i class="fas fa-map-marker-alt me-1"></i> <%= item.location %></span>
                                            <span><i class="fas fa-calendar me-1"></i> <%= new Date(item.date).toLocaleDateString() %></span>
                                        </div>
                                        <div class="d-flex justify-content-between align-items-center">
                                            <span class="text-muted small"><i class="fas fa-tag me-1"></i> <%= item.category %></span>
                                            <div>
                                                <button class="btn btn-sm btn-outline-primary" onclick="app.contactAboutItem('<%= item._id %>')">
                                                    <i class="fas fa-envelope me-1"></i> Contact
                                                </button>
                                                <% if (item.type === 'found') { %>
                                                    <button class="btn btn-sm btn-outline-success ms-1" onclick="app.claimItem('<%= item._id %>')">
                                                        <i class="fas fa-hand-holding-heart me-1"></i> Claim
                                                    </button>
                                                <% } %>
                                            </div>
                                        </div>
                                    </div>
                                </div>
                            </div>
                        </div>
                    <% }); %>
                <% } else { %>
                    <div class="col-12 text-center py-5">
                        <i class="fas fa-inbox fa-4x text-muted mb-4"></i>
                        <h4 class="text-muted mb-3">No items found</h4>
                        <p class="text-muted">Be the first to report a lost or found item!</p>
                    </div>
                <% } %>
            </div>
            <div class="text-center mt-4">
                <button class="btn btn-primary btn-lg" id="loadMoreBtn">
                    <i class="fas fa-plus me-2"></i> Load More Items
                </button>
            </div>
        </div>
    </section>

    <!-- Footer -->
    <footer>
        <div class="container">
            <div class="row">
                <div class="col-md-4 mb-4">
                    <h5 class="mb-3">Lost-n-Found</h5>
                    <p class="mb-0">Helping reunite students with their lost belongings</p>
                </div>
                <div class="col-md-4 mb-4">
                    <h5 class="mb-3">Quick Links</h5>
                    <ul class="list-unstyled">
                        <li class="mb-2"><a href="/" class="text-light text-decoration-none"><i class="fas fa-arrow-right me-2"></i>Home</a></li>
                        <li class="mb-2"><a href="#" class="text-light text-decoration-none" data-bs-toggle="modal" data-bs-target="#postLostModal"><i class="fas fa-arrow-right me-2"></i>Report Lost Item</a></li>
                        <li class="mb-2"><a href="#" class="text-light text-decoration-none" data-bs-toggle="modal" data-bs-target="#postFoundModal"><i class="fas fa-arrow-right me-2"></i>Report Found Item</a></li>
                        <li class="mb-2"><a href="/admin/login" class="text-light text-decoration-none"><i class="fas fa-arrow-right me-2"></i>Admin Login</a></li>
                    </ul>
                </div>
                <div class="col-md-4 mb-4">
                    <h5 class="mb-3">Contact</h5>
                    <p class="mb-2"><i class="fas fa-envelope me-2"></i> lost_n_found@gmail.com</p>
                    <p class="mb-2"><i class="fas fa-phone me-2"></i> 915341XXXX</p>
                    <p class="mb-0"><i class="fas fa-map-marker-alt me-2"></i> Galgotias University</p>
                </div>
            </div>
            <hr class="bg-light my-4">
            <div class="text-center">
                <p class="mb-0">&copy; Lost-n-Found System. All rights reserved.</p>
            </div>
        </div>
    </footer>

    <!-- Post Lost Item Modal -->
    <div class="modal fade" id="postLostModal" tabindex="-1">
        <div class="modal-dialog modal-lg">
            <div class="modal-content">
                <div class="modal-header">
                    <h5 class="modal-title">Report Lost Item</h5>
                    <button type="button" class="btn-close" data-bs-dismiss="modal"></button>
                </div>
                <div class="modal-body">
                    <form id="lostItemForm">
                        <div class="mb-3">
                            <label for="lostItemName" class="form-label">Item Name *</label>
                            <input type="text" class="form-control" id="lostItemName" required>
                        </div>
                        <div class="mb-3">
                            <label for="lostDescription" class="form-label">Description *</label>
                            <textarea class="form-control" id="lostDescription" rows="3" required></textarea>
                        </div>
                        <div class="row">
                            <div class="col-md-6 mb-3">
                                <label for="lostCategory" class="form-label">Category *</label>
                                <select class="form-select" id="lostCategory" required>
                                    <option value="">Select Category</option>
                                    <option value="Books">Books</option>
                                    <option value="Electronics">Electronics</option>
                                    <option value="ID Cards">ID Cards</option>
                                    <option value="Clothing">Clothing</option>
                                    <option value="Accessories">Accessories</option>
                                    <option value="Others">Others</option>
                                </select>
                            </div>
                            <div class="col-md-6 mb-3">
                                <label for="lostLocation" class="form-label">Location Lost *</label>
                                <input type="text" class="form-control" id="lostLocation" required>
                            </div>
                        </div>
                        <div class="row">
                            <div class="col-md-6 mb-3">
                                <label for="lostDate" class="form-label">Date Lost *</label>
                                <input type="date" class="form-control" id="lostDate" required>
                            </div>
                            <div class="col-md-6 mb-3">
                                <label for="lostImage" class="form-label">Item Image</label>
                                <input type="file" class="form-control" id="lostImage" accept="image/*">
                                <div class="mt-1">
                                    <small id="lostFileInfo" class="text-muted small">No file selected (Max: 5MB, Images only)</small>
                                </div>
                                <div id="lostFileInfoPreview"></div>
                            </div>
                        </div>
                        <div class="mb-3">
                            <label class="form-label">Contact Information *</label>
                            <div class="row">
                                <div class="col-md-4 mb-3">
                                    <input type="text" class="form-control" placeholder="Your Name" required>
                                </div>
                                <div class="col-md-4 mb-3">
                                    <input type="email" class="form-control" placeholder="Email" required>
                                </div>
                                <div class="col-md-4 mb-3">
                                    <input type="tel" class="form-control" placeholder="Phone">
                                </div>
                            </div>
                        </div>
                        <div class="form-check mb-3">
                            <input class="form-check-input" type="checkbox" id="lostNotifications">
                            <label class="form-check-label" for="lostNotifications">
                                Notify me about potential matches
                            </label>
                        </div>
                    </form>
                </div>
                <div class="modal-footer">
                    <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">Cancel</button>
                    <button type="button" class="btn btn-primary" id="submitLostItem">Submit Report</button>
                </div>
            </div>
        </div>
    </div>

    <!-- Post Found Item Modal -->
    <div class="modal fade" id="postFoundModal" tabindex="-1">
        <div class="modal-dialog modal-lg">
            <div class="modal-content">
                <div class="modal-header">
                    <h5 class="modal-title">Report Found Item</h5>
                    <button type="button" class="btn-close" data-bs-dismiss="modal"></button>
                </div>
                <div class="modal-body">
                    <form id="foundItemForm">
                        <div class="mb-3">
                            <label for="foundItemName" class="form-label">Item Name *</label>
                            <input type="text" class="form-control" id="foundItemName" required>
                        </div>
                        <div class="mb-3">
                            <label for="foundDescription" class="form-label">Description *</label>
                            <textarea class="form-control" id="foundDescription" rows="3" required></textarea>
                        </div>
                        <div class="row">
                            <div class="col-md-6 mb-3">
                                <label for="foundCategory" class="form-label">Category *</label>
                                <select class="form-select" id="foundCategory" required>
                                    <option value="">Select Category</option>
                                    <option value="Books">Books</option>
                                    <option value="Electronics">Electronics</option>
                                    <option value="ID Cards">ID Cards</option>
                                    <option value="Clothing">Clothing</option>
                                    <option value="Accessories">Accessories</option>
                                    <option value="Others">Others</option>
                                </select>
                            </div>
                            <div class="col-md-6 mb-3">
                                <label for="foundLocation" class="form-label">Location Found *</label>
                                <input type="text" class="form-control" id="foundLocation" required>
                            </div>
                        </div>
                        <div class="row">
                            <div class="col-md-6 mb-3">
                                <label for="foundDate" class="form-label">Date Found *</label>
                                <input type="date" class="form-control" id="foundDate" required>
                            </div>
                            <div class="col-md-6 mb-3">
                                <label for="foundImage" class="form-label">Item Image</label>
                                <input type="file" class="form-control" id="foundImage" accept="image/*">
                                <div class="mt-1">
                                    <small id="foundFileInfo" class="text-muted small">No file selected (Max: 5MB, Images only)</small>
                                </div>
                                <div id="foundFileInfoPreview"></div>
                            </div>
                        </div>
                        <div class="mb-3">
                            <label for="storageLocation" class="form-label">Storage Location</label>
                            <input type="text" class="form-control" id="storageLocation" placeholder="Where is the item currently stored?">
                        </div>
                        <div class="mb-3">
                            <label class="form-label">Your Contact Information *</label>
                            <div class="row">
                                <div class="col-md-4 mb-3">
                                    <input type="text" class="form-control" placeholder="Your Name" required>
                                </div>
                                <div class="col-md-4 mb-3">
                                    <input type="email" class="form-control" placeholder="Email" required>
                                </div>
                                <div class="col-md-4 mb-3">
                                    <input type="tel" class="form-control" placeholder="Phone">
                                </div>
                            </div>
                        </div>
                    </form>
                </div>
                <div class="modal-footer">
                    <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">Cancel</button>
                    <button type="button" class="btn btn-primary" id="submitFoundItem">Submit Report</button>
                </div>
            </div>
        </div>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/js/bootstrap.bundle.min.js"></script>
    <script src="/js/main.js"></script>
</body>
</html>